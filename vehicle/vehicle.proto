syntax = "proto3";
package vehicle;
option go_package = "./vehicle";

message Message {
    string car_number = 1;
    string car_direction = 2;
    int32 vote = 3;
}

service ChatService {
    rpc SendRequest(Message) returns (Message);
    rpc SendResponse(Message) returns (Message);
}